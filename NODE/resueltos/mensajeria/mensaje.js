class Mensaje {
  constructor(emisor, destinatario, contenido, fecha) {
    this.emisor = emisor;
    this.destinatario = destinatario;
    this.contenido = contenido;
    this.fecha = fecha;
  }
};

export default Mensaje;